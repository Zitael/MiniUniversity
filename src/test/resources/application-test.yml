spring:
  liquibase:
    change-log: classpath:databaseChangeLog_test.xml
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test_mem
    username: sa
    password:
  jpa:
    show-sql: true